# LC Helper Environment Configuration
# Copy this file to .env and fill in your actual values

# OpenAI Configuration
OPEN_AI_KEY=your_openai_api_key_here

# JWT Configuration
SECRET_KEY=your_jwt_secret_key_here_change_this_in_production

# Database Configuration
# Choose one database type: postgresql, mysql, or sqlite
DATABASE_TYPE=postgresql

# PostgreSQL Configuration (default)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=lc_helper
DB_USER=postgres
DB_PASSWORD=your_secure_postgres_password_here

# MySQL Configuration (only needed if DATABASE_TYPE=mysql)
# DB_HOST=localhost
# DB_PORT=3306
# DB_NAME=lc_helper
# DB_USER=root
# DB_PASSWORD=your_secure_mysql_password_here

# SQLite Configuration (only needed if DATABASE_TYPE=sqlite)
# No additional configuration needed for SQLite

# Security Notes:
# 1. Never commit .env files to version control
# 2. Use strong, unique passwords for database
# 3. Change default database usernames
# 4. Use environment-specific .env files (dev, staging, prod)
# 5. Rotate secrets regularly in production
# 6. Ensure PostgreSQL is properly configured with SSL in production
